#include <stdio.h>
/* Es algo similar al ejercicio 1*/
    int main(){
        int x;
        int g,j;
        scanf("%d" ,&x);
        for(g = x; g >= 1; g--){
            for(j = 0; j < g; j++){
                printf("%d ", j + 1);
            }
            printf(" ");
            for(j = 1; j <= x - g; j++){
                printf(" ");
            }
            for(j = 1; j <= x - g; j++){
                printf(" ");
            }
            for(j = g ; j > 0; j--){
                for(j = 1; j < x - g; j++){
                printf(" ");
            }
            for(j = 1; j <= x - i; j++){
                printf(" ");
            }
            for(j = g ; j > 0 ; j--){
                printf("%d ", j);
                }
            }
        printf("\n");
        }
        return 0;
    }
